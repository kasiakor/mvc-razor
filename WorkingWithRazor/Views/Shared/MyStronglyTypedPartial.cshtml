@model IEnumerable<string>

    <div>
        This is the message from the strongly typed view
        <ul>
            @foreach(string str in Model)
            {
                <li>@str</li>
            }
        </ul>
    </div>
